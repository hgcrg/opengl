project(01_qt)
aux_source_directory(. SRC)
include(${QT_USE_FILE})
set(CSS 01_qt.css)
qt4_wrap_cpp(MOCS Frame.h)
file(COPY ${CSS} DESTINATION ${CMAKE_BINARY_DIR}/bin)
add_executable(${PROJECT_NAME} ${SRC} ${CSS} ${MOCS})
target_link_libraries(${PROJECT_NAME} ${QT_LIBRARIES})
